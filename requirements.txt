# PAQUETEX — dependencias de runtime (rebuild PaqueteXv.2)

fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
jinja2==3.1.2
python-multipart==0.0.6
python-dotenv==1.0.0
# Cliente HTTP para el conector real de LIWA (SMS).
httpx==0.25.2

# Hashing de contraseñas del staff (bcrypt directo, sin passlib).
bcrypt==4.1.3
# Firma de la cookie de sesión (SessionMiddleware de Starlette).
itsdangerous==2.1.2
